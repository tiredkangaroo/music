version: "2"
sql:
  - schema: "schema.sql"
    queries: "queries.sql"
    engine: "postgresql"
    gen:
      go:
        out: "db"
        package: "db"
        sql_package: "pgx/v5"
        emit_json_tags: true
overrides:
  go: null
plugins: []
rules: []
options: {}
